library(haven)
training_pl <- read_sas("C:/Users/AdminQuark/Nextcloud/017.ScoringAvanzado/BBDD_por_modelo/01PASIVOLIMPIO/training_pl.sas7bdat")
View(training_pl)
demo()
6.25+2.25+0.25+2.25+6.25
/6
(6.25+2.25+0.25+2.25+6.25)/6
91/6
91/6-3.5*3.5
1+4+9+16+25+46
91/6-(3.5)^2
2.5^2+2.25+0.25+0.25+2.25+6.25
(2.5^2+2.25+0.25+0.25+2.25+6.25)/6
72-112+49+48+49-84
# Pie Chart with Percentages
> slices <- c(26, 5, 15, 1)
> lbls <- c("Sí", "No", "Algunos", "NC")
> pct <- round(slices/sum(slices)*100)
> lbls <- paste(lbls, pct) # add percents to labels
> lbls <- paste(lbls,"%",sep="") # ad % to labels
> pie(slices,labels = lbls, col=rainbow(length(lbls)),
+     main="¿Conoces los diversos GT?")
# Pie Chart with Percentages
> slices <- c(26, 5, 15, 1)
> lbls <- c("Sí", "No", "Algunos", "NC")
> pct <- round(slices/sum(slices)*100)
> lbls <- paste(lbls, pct) # add percents to labels
> lbls <- paste(lbls,"%",sep="") # ad % to labels
> pie(slices,labels = lbls, col=rainbow(length(lbls)),
+     main="¿Conoces los diversos GT?")
# Pie Chart with Percentages
> slices <- c(26, 5, 15, 1)
> lbls <- c("Sí", "No", "Algunos", "NC")
> pct <- round(slices/sum(slices)*100)
> lbls <- paste(lbls, pct) # add percents to labels
> lbls <- paste(lbls,"%",sep="") # ad % to labels
> pie(slices,labels = lbls, col=rainbow(length(lbls)),
+     main="¿Conoces los diversos GT?")
# Pie Chart with Percentages
> slices <- c(26, 5, 15, 1)
> lbls <- c("Sí", "No", "Algunos", "NC")
> pct <- round(slices/sum(slices)*100)
> lbls <- paste(lbls, pct) # add percents to labels
> lbls <- paste(lbls,"%",sep="") # ad % to labels
> pie(slices,labels = lbls, col=rainbow(length(lbls)),main="¿Conoces los diversos GT?")
# Pie Chart with Percentages
slices <- c(26, 5, 15, 1)
lbls <- c("Sí", "No", "Algunos", "NC") pct <- round(slices/sum(slices)*100)
> lbls <- paste(lbls, pct) # add percents to labels
> lbls <- paste(lbls,"%",sep="") # ad % to labels
> pie(slices,labels = lbls, col=rainbow(length(lbls)),main="¿Conoces los diversos GT?")
# Pie Chart with Percentages
slices <- c(26, 5, 15, 1)
lbls <- c("Sí", "No", "Algunos", "NC") pct <- round(slices/sum(slices)*100)
lbls <- paste(lbls, pct) # add percents to labels
lbls <- paste(lbls,"%",sep="") # ad % to labels
pie(slices,labels = lbls, col=rainbow(length(lbls)),main="¿Conoces los diversos GT?")
# Pie Chart with Percentages
slices <- c(26, 5, 15, 1)
lbls <- c("Sí", "No", "Algunos", "NC")                                                                                 pct <- round(slices/sum(slices)*100)
lbls <- paste(lbls, pct) # add percents to labels
lbls <- paste(lbls,"%",sep="") # ad % to labels
pie(slices,labels = lbls, col=rainbow(length(lbls)),main="¿Conoces los diversos GT?")
# Pie Chart with Percentages
slices <- c(26, 5, 15, 1)
lbls <- c("Sí", "No", "Algunos", "NC")                                                                                 pct <- round(slices/sum(slices)*100)
lbls <- paste(lbls, pct) # add percents to labels
lbls <- paste(lbls,"%",sep="") # ad % to labels
pie(slices,labels = lbls, col=rainbow(length(lbls)),main="¿Conoces los diversos GT?")
slices <- c(26, 5, 15, 1)
lbls <- c("Sí", "No", "Algunos", "NC")
pct <- round(slices/sum(slices)*100)
lbls <- paste(lbls, pct) # add percents to labels
lbls <- paste(lbls,"%",sep="") # ad % to labels
pie(slices,labels = lbls, col=rainbow(length(lbls)),main="¿Conoces los diversos GT?")
2+2
getwd()
setwd("../proyectos/cursos/r-course/scripts/tema1/")
getwd()
